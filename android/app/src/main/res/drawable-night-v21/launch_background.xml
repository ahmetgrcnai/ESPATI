<?xml version="1.0" encoding="utf-8"?>
<!-- Step 65 — reverted to Flutter's plain, unbranded default. Native
     splash customization (flutter_native_splash) has been removed entirely;
     all branding now lives in the Dart-level SplashScreen widget. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@android:color/black" />
</layer-list>
